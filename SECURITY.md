# سياسة الأمان — OneSecret

## نطاق هذه الوثيقة

تصف هذه السياسة **المعمارية الحالية في `main`**، لا الإصدارات القديمة فقط. OneSecret يدعم الآن ثلاث فئات بيانات بحدود ثقة مختلفة: النصوص، الملفات، والصور.

## نموذج الثقة حسب نوع البيانات

| النوع | مكان التشفير | هل يرى Backend البيانات الأصلية؟ | أين مادة فك التشفير؟ |
|---|---|---:|---|
| Text | المتصفح — AES-256-GCM | لا في المسار الحالي | مفتاح AES داخل URL Fragment |
| File | المتصفح — AES-256-GCM + RSA-OAEP/SHA-256 | لا في المسار الحالي | RSA private key + claim token داخل URL Fragment |
| Image | Backend — Single DES-CBC + PKCS#7 | نعم، أثناء عملية التشفير | مفتاح DES في رابط المشاركة ولا يُحفظ في DB |

لا يصح وصف المشروع كله بأنه Zero Knowledge أو End-to-End Encryption من دون هذا التفصيل؛ لأن مسار الصور يمر بالـBackend كبيانات أصلية قبل التشفير.

## النصوص

المتصفح يولد مفتاح AES-256 وNonce جديدًا بطول 12 بايت ويشفر النص باستخدام AES-GCM قبل إرسال الحزمة إلى الخادم. الخادم يخزن ciphertext وnonce والـmetadata اللازمة للمشاركة، ولا يخزن مفتاح AES للمسار الحالي.

AES-GCM يوفر السرية والتحقق من سلامة/أصالة ciphertext. يجب عدم إعادة استخدام نفس Nonce مع نفس المفتاح.

## الملفات

الملف يُشفّر محليًا باستخدام مفتاح AES-256 عشوائي وAES-GCM. RSA لا يشفر الملف كاملًا؛ RSA-OAEP/SHA-256 يشفر مفتاح AES فقط. المفتاح الخاص RSA لا يُرسل إلى Backend في المسار الحالي.

يحصل الخادم على envelope مشفر ورمز claim مشتقًا/مخزنًا كبصمة مناسبة للعقد الحالي، ثم تُمسح مكونات envelope الحساسة بعد نجاح مسار الاستهلاك.

## الصور

مسار الصور تعليمي ويستخدم Single DES في وضع CBC مع PKCS#7. هذا المسار لا يملك ضمانات AES-GCM نفسها، وDES خوارزمية Legacy ذات مفتاح فعال صغير ولا يجب تقديمها كاختيار حديث للإنتاج.

Backend يستقبل الصورة الأصلية عند إنشاء مشاركة DES، ثم يخزن IV وciphertext مؤقتًا ولا يخزن مفتاح DES. لذلك HTTPS ضروري لحماية نقل الصورة إلى الخادم.

قبل التشفير يتحقق Backend من النوع المعلن **ومن Magic Bytes** للصورة: توقيع PNG القياسي أو بادئة JPEG. لذلك لا يكفي إرسال ملف عشوائي مع MIME من نوع `image/png` أو `image/jpeg` كي يُقبل في مسار DES.

## URL Fragment

المفاتيح أو المواد الحساسة الموضوعة بعد `#` في رابط المشاركة لا يرسلها المتصفح تلقائيًا ضمن طلب HTTP العادي إلى الخادم. مع ذلك، الرابط الكامل نفسه **حساس**: من يحصل عليه يستطيع امتلاك مادة فك التشفير أو الاستلام الخاصة بالمشاركة.

لا تنشر رابطًا عاملًا في issue أو commit أو screenshot أو log.

## HTTPS

التشفير Client-side لا يلغي الحاجة إلى HTTPS. TLS يحمي:

- metadata والطلبات والاستجابات أثناء النقل.
- الحزم المشفرة من التعديل على مستوى الشبكة قبل تحقق التطبيق.
- مسار الصور الذي يرسل plaintext image إلى Backend.
- واجهة التطبيق نفسها من الاستبدال أثناء النقل في بيئة النشر.

## بيانات لا يجب تسجيلها

يجب ألا تظهر في logs أو Git أو Issues أو Pull Requests:

- plaintext لأي نص أو ملف أو صورة حقيقية.
- AES keys أو RSA private keys أو DES keys.
- claim tokens أو Secret Codes أو Cancel Codes الفعلية.
- روابط مشاركة كاملة تحتوي `#k=` أو `#t=`.
- قيم `ONESECRET_ENCRYPTION_KEY` أو Database credentials.
- قواعد بيانات محلية أو ملفات `.env` حقيقية.

استخدم بيانات اختبار فقط.

## الروابط المؤقتة والاستهلاك

الانتهاء الزمني يقلل مدة التعرض لكنه لا يسحب نسخة سبق أن فكها المستلم. كذلك لا يستطيع المشروع منع screenshot أو copy أو backup خارج النظام بعد فك المحتوى.

مسارات الملفات والصور مصممة كسيناريو one-time، لكن أي ضمان متعلق بالتزامن يجب تقييمه على مستوى العملية الذرية الفعلية قبل وصفه بأنه حصري 100% تحت جميع السباقات. التوثيق النهائي يجب أن يلتزم بما تثبته الاختبارات فقط.

## Rate limiting

OneSecret يملك محدد طلبات داخل ذاكرة عملية الخادم. هذا مناسب للتشغيل أحادي النسخة، لكنه ليس Rate Limit موزعًا بين عدة replicas. التشغيل متعدد النسخ يحتاج مخزنًا مشتركًا أو خدمة مناسبة.

في Client Text API توجد حماية مستقلة لمحاولات التخمين الخاطئة:

- Secret Code: حتى 5 محاولات خاطئة ضمن نافذة 10 دقائق لكل مصدر ولكل مشاركة.
- Cancel Code: حتى 3 محاولات خاطئة ضمن نافذة 15 دقيقة لكل مصدر ولكل مشاركة.
- Cancel Code global bucket: حتى 5 محاولات خاطئة ضمن نافذة 15 دقيقة لكل مشاركة عبر bucket مشترك داخل نسخة الخادم.

قبل التحقق من الرمز يجري فحص حالة bucket دون استهلاك محاولة جديدة؛ وعند فشل الرمز تُستهلك محاولة. عند بلوغ الحد يعيد الخادم `429` مع `Retry-After`.

## Legacy compatibility

لا تزال بعض Server-side APIs القديمة موجودة للتوافق مع روابط/سلوك سابق، بما فيها AES server-side وبعض مسارات RSA التعليمية. وجودها لا يعني أنها المسارات الأساسية الجديدة.

قبل V2 يجب اتخاذ قرار صريح لكل Legacy endpoint: إبقاؤه خلف Compatibility boundary أو تعطيله أو حذفه بعد إثبات عدم الحاجة إليه.

## الإبلاغ المسؤول

عند اكتشاف مشكلة أمنية:

1. لا تنشر أسرارًا أو روابط صالحة أو بيانات شخصية.
2. استخدم payloads اختبارية فقط.
3. اذكر الأثر والخطوات المختصرة لإعادة الإنتاج.
4. إن كان GitHub Private Vulnerability Reporting مفعّلًا فاستخدمه؛ وإلا افتح issue عام بلا تفاصيل حساسة.

## ما لا يدعيه OneSecret

OneSecret لا يدعي:

- الحماية من جهاز مستلم مخترق.
- منع النسخ أو التصوير بعد فك التشفير.
- أن DES آمن للاستخدام الحديث.
- أن URL Fragment يجعل مشاركة الرابط نفسه غير حساسة.
- أن Rate Limiting الحالي موزع بين عدة خوادم.
- أن جميع المسارات لها Trust Boundary واحدة.

القاعدة: **نصف ما ينفذه الكود والاختبارات فعليًا فقط، من دون توسيع الادعاء الأمني.**

# OneSecret — قائمة العمل

- [x] المرحلة 1: تثبيت بنية المشروع بثلاث طبقات وتوثيق مسؤولية كل ملف أساسي في README.
  - ناتج القبول: استُبدل تصميم tRPC وDrizzle بعد اعتماد Python؛ البنية الفعلية هي React وFastAPI وSQLAlchemy.

- [x] المرحلة 2: إنشاء جدول `secrets` الذي يخزن المعرّف العشوائي وciphertext وnonce ووقت الإنشاء والانتهاء وحالة الاستخدام فقط.
  - ناتج القبول: استُبدل Drizzle بنموذج SQLAlchemy المنجز؛ لا يحتوي الجدول على النص الأصلي أو مفتاح التشفير.

- [x] المرحلة 2: إضافة إجراءات tRPC عامة لإنشاء سجل السر، واسترداد حالته، واستهلاكه ذريًا بعد التحقق من عدم الاستخدام وعدم الانتهاء.
  - ناتج القبول: استُبدل tRPC بمسارات FastAPI المنجزة، مع استهلاك ذري وحالات عامة.

- [x] المرحلة 3: بناء وحدة Web Crypto API في الواجهة لتوليد مفتاح AES-GCM وnonce عشوائيين وتشفير النص داخل المتصفح.
  - ناتج القبول: استُبدل Web Crypto بتشفير AES-GCM داخل Python؛ الخادم موثوق ولا يحفظ النص الواضح.

- [x] المرحلة 3: بناء الصفحة العامة الثنائية اللغة لإنشاء رسالة، واختيار مدة الصلاحية، وإنشاء رابط مشاركة بصيغة `secretId#key`.
  - ناتج القبول: استُبدل رابط Fragment برابط `/s/{id}`؛ صفحة الإنشاء الثنائية اللغة منجزة.

- [x] المرحلة 4: بناء صفحة استقبال السر مع زر Reveal صريح لا يستهلك السر عند فتح الصفحة.
  - ناتج القبول: صفحة Reveal منجزة؛ لا تعرض النص ولا تستهلكه قبل النقر الصريح.

- [x] المرحلة 4: فك تشفير السر في متصفح المستقبل وعرض الحالات الآمنة للروابط الصحيحة والمستهلكة والمنتهية وغير الصحيحة وفشل التحقق.
  - ناتج القبول: استُبدل فك المتصفح بفك Python؛ صفحة Reveal تعرض حالات آمنة والنص بعد الاستهلاك الذري.

- [x] المرحلة 5: كتابة اختبارات Vitest لمنطق التخزين والحالة والاستهلاك الذري وانتهاء الصلاحية.
  - ناتج القبول: استُبدلت اختبارات منطق Python بـpytest، وهي تغطي الحفظ والحالة والانتهاء والاستهلاك الذري. يحتفظ Vitest باختبار قالب المشروع الأساسي فقط.

- [x] المرحلة 5: تنفيذ فحص النوع والبناء، ومراجعة الواجهة على سطح المكتب والهاتف، والتحقق من حالات التحميل والخطأ.
  - ناتج القبول: ينجح `pnpm test` و`pnpm check` وتظهر الواجهة بصورة سليمة في لقطات الاختبار.

- [x] المرحلة 6: تحديث README لتوثيق معمارية الطبقات، حدود الحماية، وخطوات التشغيل والاختبار.
  - ناتج القبول: يعكس README نموذج Python الفعلي، والطبقات الثلاث، وما يحميه المشروع وما لا يحميه، وطريقة التشغيل والاختبارات ووضع النشر الحالي.

- [x] مراجعة واجهتي Create وReveal على شاشة هاتف وتوثيق النتيجة.
  - ناتج القبول: اختُبرت الواجهتان على عرض 390×844؛ بقيت النصوص والحقول والأزرار قابلة للقراءة ولم يظهر تجاوز أفقي.

- [x] تخطيط فقط: إعداد وثيقة مراحل تنفيذ كاملة قبل كتابة أي كود إضافي أو بناء أي واجهة.
  - ناتج القبول: لكل مرحلة هدف ومخرجات ومعيار قبول وحدود واضحة تمنع التشتت.

- [x] تخطيط فقط: توثيق بنية الطبقات الثلاث ومسؤولية كل ملف مستقبلي دون إنشاء ملفات تنفيذية جديدة.
  - ناتج القبول: يتضح مكان كل وظيفة مستقبلية ولا توجد مسؤوليات مكررة بين الواجهة والخادم والبيانات.

- [x] تخطيط فقط: اعتماد PROJECT_PLAN.md كمرجع وحيد للمراحل قبل بدء أي تنفيذ برمجي.
  - ناتج القبول: لا تبدأ أي مرحلة لاحقة إلا بطلب صريح من المستخدم وبالرجوع إلى هدفها ومعيار قبولها.

- [x] تخطيط فقط: تحديث المعمارية لتكون الواجهة React وTypeScript وTailwind CSS، والخادم وقاعدة البيانات والاختبارات بـ Python.
  - ناتج القبول: توضح الخطة مكان Python وReact بدقة، مع توثيق أن الخادم هو من يشفّر ويفك التشفير في النموذج الجديد.

- [x] تخطيط فقط: تحديث نموذج الأمان لتشفير وفك تشفير الرسائل داخل خادم Python باستخدام AES-GCM.
  - ناتج القبول: توضح الخطة أن الخادم موثوق ويرى النص لحظيًا، لكنه لا يخزنه كنص واضح أو يحتفظ بالمفتاح داخل قاعدة البيانات.

- [x] تخطيط فقط: اعتماد أسماء المراحل النهائية وفق React وTypeScript وTailwind CSS وPython وFastAPI وAES-GCM وSQLAlchemy.
  - ناتج القبول: يصبح PROJECT_PLAN.md المرجع النهائي بأسماء مراحل ثابتة قبل بدء التنفيذ.

- [x] تخطيط فقط: إعادة توليد PROJECT_PLAN.md بأسماء المراحل المعتمدة والتقنيات النهائية.
  - ناتج القبول: يحتوي الملف على التقنيات والطبقات وعناوين المراحل فقط، من دون البدء في أي تنفيذ برمجي.

- [x] عرض الهيكل النصي للواجهة والخادم والبيانات قبل إنشاء أي مجلد أو ملف جديد.
  - ناتج القبول: يعتمد المستخدم هيكل الملفات ومسؤولية كل ملف صراحةً قبل تجهيز البيئة الفعلية.

- [x] تجهيز بيئة React وTypeScript وTailwind CSS داخل `frontend/` من دون صفحات وظائف الرسائل.
  - ناتج القبول: ينجح فحص TypeScript للواجهة وتظهر صفحة بيئة حيادية فقط.

- [x] تجهيز بيئة Python وFastAPI وSQLAlchemy داخل `backend/` من دون مسارات أسرار أو تشفير.
  - ناتج القبول: يمكن استيراد تطبيق FastAPI وملفات الإعداد والنماذج من دون أخطاء.

- [x] إنشاء ملفات الطبقات المعتمدة ذات هياكل أولية وتعليقات مسؤولية فقط.
  - ناتج القبول: توجد الملفات المعتمدة في الهيكل، ولا تحتوي على منطق إنشاء أو تشفير أو كشف الرسائل.

- [x] إنشاء مجلد `docs/` وملف توثيق مرحلي لمرحلة قاعدة البيانات.
  - ناتج القبول: يشرح الملف الهدف والحدود والملفات والحقول والاختبارات وما لا يدخل في المرحلة بلغة واضحة.

- [x] تعريف اتصال SQLAlchemy ونموذج جدول `secrets` من دون نص أصلي أو مفتاح تشفير.
  - ناتج القبول: يحتوي النموذج على المعرّف وciphertext وnonce وأوقات الإنشاء والانتهاء والاستخدام فقط.

- [x] كتابة اختبار Python لنموذج البيانات باستخدام قاعدة بيانات اختبار محلية.
  - ناتج القبول: يثبت الاختبار إنشاء السجل وقيم الحقول وغياب حقول النص الأصلي ومفتاح التشفير.

- [x] تحديث توثيق مرحلة البيانات وtodo.md بعد نجاح الاختبارات.
  - ناتج القبول: يستطيع الطالب الرجوع إلى `docs/` وفهم ما أُنجز وما بقي للمرحلة التالية.

- [x] مراجعة ملفات المقرر واستخراج المنطق المشترك لخوارزمية AES والكود المرجعي القابل للشرح.
  - ناتج القبول: يوثق ملف المرحلة ما يأتي من المقرر وما يضاف تقنيًا لدعم AES-GCM.

- [x] توثيق خطة التشفير الفرعية داخل `docs/03-crypto.md` قبل كتابة منطق التشفير.
  - ناتج القبول: يحدد الملف خطوات توليد المفتاح وNonce والتشفير وفك التشفير وحالات الاختبار وحدود كل خطوة.

- [x] تنفيذ خطوة توليد مفتاح AES-256 والتحقق من صحته داخل Python.
  - ناتج القبول: يمر اختبار المفتاح الصحيح ويرفض المفتاح الناقص أو غير الصالح برسالة واضحة.

- [x] تنفيذ خطوة توليد Nonce جديد لكل عملية تشفير واختبار اختلافه.
  - ناتج القبول: يثبت الاختبار أن كل Nonce بطول صحيح وأنه لا يعاد استخدامه في عينات الاختبار.

- [x] تنفيذ تشفير AES-GCM للنصوص وفك التشفير الصحيح داخل `crypto.py`.
  - ناتج القبول: يعيد فك التشفير النص الأصلي عند استخدام المفتاح والـNonce والبيانات الصحيحة.

- [x] اختبار حالات الفشل: مفتاح خاطئ وNonce خاطئ وciphertext معدل وبيانات غير نصية.
  - ناتج القبول: تفشل كل حالة بأمان من دون إعادة نص خاطئ أو كشف تفاصيل حساسة.

- [x] تحديث توثيق مرحلة التشفير وtodo.md بعد نجاح جميع الاختبارات.
  - ناتج القبول: يستطيع الطالب شرح مسار التشفير وحالات النجاح والفشل والفرق بين AES المقرر وGCM المضاف.

- [x] توثيق دورة حياة السر وخطة FastAPI داخل `docs/04-api-lifecycle.md` قبل كتابة المسارات.
  - ناتج القبول: يشرح الملف حالة السر وعمليات create وstatus وreveal وحالات الخطأ والتزامن وما لا يدخل في المرحلة.

- [x] تعريف نماذج طلبات وردود FastAPI الخاصة بالسر من دون كشف تفاصيل حساسة.
  - ناتج القبول: تقبل نماذج الإنشاء `secret_id` و`plaintext` ووقت الانتهاء لأن Python تشفّر داخل الخادم، ولا تحتوي نماذج الرد العامة على ciphertext أو nonce أو مفتاح.

- [x] تنفيذ إنشاء سجل سر وفحص حالته دون كشف ciphertext.
  - ناتج القبول: ينشئ منطق الخدمة سجلًا مشفّرًا فقط ويستنتج الحالات active وexpired وused وmissing؛ وتثبت مسارات FastAPI في الخطوة التالية أن الرد الخارجي لا يكشف ciphertext.

- [x] تنفيذ Reveal ذري يعيد ciphertext وnonce مرة واحدة فقط.
  - ناتج القبول: ينجح أول Reveal صحيح ويعيد النص بعد فك التشفير في خادم Python، ويرفض الثاني والمنتهي، ولا يحدث الاستهلاك عند طلب status.

- [x] اختبار التزامن وحالات FastAPI الناجحة والفاشلة.
  - ناتج القبول: عند محاولة استهلاك السر مرتين في الوقت نفسه ينجح طلب واحد فقط، وتنجح جميع اختبارات API.

- [x] تحديث توثيق API وtodo.md بعد اكتمال الاختبارات.
  - ناتج القبول: يشرح `docs/04-api-lifecycle.md` كل قرار ونتيجة الاختبار، ويستطيع الطالب مناقشته.

- [x] توثيق تصميم صفحة إنشاء السر وحالاتها داخل `docs/05-create-interface.md` قبل كتابة React.
  - ناتج القبول: يشرح الملف الحقول ومسار البيانات واللغتين والحالات والاختبارات وما يؤجل لصفحة Reveal.

- [x] بناء ملف i18n ودعم تبديل العربية RTL والإنجليزية LTR.
  - ناتج القبول: تتغير جميع نصوص الصفحة والاتجاه عند تبديل اللغة.

- [x] بناء نموذج React لإنشاء النص واختيار مدة الصلاحية والتحقق من الإدخال.
  - ناتج القبول: يمنع النموذج النص الفارغ ويعطّل زر الإنشاء أثناء الطلب ولا ينشئ سرًا من الواجهة وحدها.

- [x] ربط واجهة الإنشاء مع `POST /api/secrets` وعرض رابط المشاركة القابل للنسخ.
  - ناتج القبول: ينشئ الطلب الناجح رابطًا بصيغة `/s/{id}` ويعرض خطأ عامًا عند فشل الخادم.

- [x] اختبار بناء TypeScript وحالات الواجهة وتصوير النتيجة وتحديث التوثيق وtodo.md.
  - ناتج القبول: ينجح بناء الواجهة وفحص TypeScript، وتوثق حالات العربية والإنجليزية والنجاح والخطأ.

- [x] توثيق تجربة المستلم وحالات الرابط داخل `docs/06-reveal-interface.md` قبل كتابة صفحة Reveal.
  - ناتج القبول: يشرح الملف حالة active وused وexpired وmissing، وزر Reveal الصريح، وما لا تحميه الصفحة.

- [x] إضافة دوال API لفحص الحالة والكشف الصريح في واجهة React.
  - ناتج القبول: تفصل الواجهة بين `GET status` و`POST reveal` ولا تستهلك السر عند فتح الرابط.

- [x] بناء صفحة Reveal ثنائية اللغة وتسجيل مسار `/s/:id` داخل React.
  - ناتج القبول: تعرض الصفحة حالة الرابط، وزر Reveal للسر النشط فقط، ورسائل واضحة للحالات الأخرى.

- [x] عرض النص بعد Reveal بنجاح ومنع محاولة كشف ثانية من الواجهة.
  - ناتج القبول: يظهر النص مرة واحدة في الواجهة ويتحول المسار إلى حالة used عند محاولة ثانية.

- [x] اختبار الرابط الصحيح والمستهلك والمنتهي والمفقود وفشل الخادم وتحديث docs وtodo.md.
  - ناتج القبول: تنجح اختبارات البناء والخادم، وتوثق المعاينة كل حالة من حالات Reveal.

- [x] إضافة أداة محلية لتوليد مفتاح AES-256 Base64 من دون حفظه في الكود أو المستودع.
  - ناتج القبول: تنشئ الأداة 32 بايت عشوائية وتطبع Base64 فقط في شاشة التنفيذ، مع توثيق كيفية وضع الناتج في الحقل الآمن.

- [x] توليد مفتاح AES-256 جديد والتحقق من صيغته قبل إدخاله في إعدادات المشروع الآمنة.
  - ناتج القبول: يفك المفتاح إلى 32 بايت، ولا يظهر في المحادثة أو الكود أو ملفات المشروع.

- [x] تهيئة خادم Python لقراءة `ONESECRET_ENCRYPTION_KEY` من البيئة والتحقق من صيغته دون طباعته.
  - ناتج القبول: يقبل الخادم مفتاح Base64 من 32 بايت ويرفض القيمة المفقودة أو غير الصالحة برسالة إعداد عامة.

- [x] توثيق إدارة المفتاح ومنع حفظه في الكود أو قاعدة البيانات أو السجلات.
  - ناتج القبول: يشرح التوثيق مصدر المفتاح ومكان استخدامه وما لا يجوز فعله به.

- [x] مرحلة التقوية: معالجة ciphertext أو nonce التالفين برسالة Reveal عامة من دون استثناء داخلي أو كشف تفاصيل.
  - ناتج القبول: يثبت اختبار API أن ciphertext التالف يعيد 410 ورسالة عامة آمنة، ولا ينهار الخادم.

- [x] مرحلة التقوية: التحقق الصارم من صيغة معرّف السر ومدة الصلاحية في واجهة API.
  - ناتج القبول: ترفض API المعرّف غير المكوّن من 48 رمزًا سداسيًا عشريًا، والوقت بلا منطقة زمنية، والمدة التي تتجاوز 24 ساعة.

- [x] مرحلة التشغيل: بناء مسار إنتاجي يقدّم React المبني مع FastAPI ويدعم فتح `/s/{id}` مباشرة.
  - ناتج القبول: نجح إنشاء السر والكشف مرة واحدة وفتح `/s/{id}` مباشرة على خدمة FastAPI موحّدة، من دون proxy تطوير.

- [x] مرحلة التشغيل: تجهيز Dockerfile للنشر يتضمن Node وPython ومكتبات Python اللازمة من دون أسرار مكتوبة.
  - ناتج القبول: يبني Dockerfile React داخل الصورة ويشغل Uvicorn على `PORT` البيئة، ولا يحتوي على قيم أسرار.

- [x] مرحلة التقوية: إعادة تنفيذ اختبارات Python وبناء React واختبار مسار الإنتاج وتوثيق النتيجة في `docs/07-hardening-deployment.md`.
  - ناتج القبول: نجحت اختبارات Python وبناء الواجهة والتدفق الموحد، وتوثقت القرارات وحدود النشر الصادقة.

- [x] مراجعة الاستكشاف: تشغيل رابط حي مؤقت للنسخة الموحدة والتحقق من ظهور واجهة OneSecret.
  - ناتج القبول: أتيح رابط موقت يعرض واجهة OneSecret الفعلية، وليس قالب المشروع القديم، لاستكشافه قبل النشر.

- [x] تدقيق شامل: إنشاء خط أساس للاختبارات والبناء وفحص النمط قبل أي إصلاح.
  - ناتج القبول: نجح `git diff --check` و`pytest` (47) وTypeScript وبناء React وVitest الأساسي؛ لا توجد نتيجة فاشلة في خط الأساس.

- [x] تدقيق شامل: مراجعة تشفير AES-GCM وخدمة الأسرار ونموذج SQLAlchemy وحالات البيانات التالفة.
  - ناتج القبول: غطت الاختبارات النصوص، والأطوال، والمفتاح والـnonce الخاطئين، والتزامن، وقاعدة البيانات. أُصلح استهلاك الرابط التالف وتوافق عنوان MySQL، ونجح فحص اتصال قاعدة بيانات المشروع.

- [x] تدقيق شامل: اختبار API للحالات الصحيحة والخاطئة وحدود المدخلات والاستجابة العامة الآمنة.
  - ناتج القبول: اختُبرت الإنشاء والحالة والكشف والتعارض والانتهاء والتلف والتزامن ومسارات API غير المعروفة؛ لا يعكس أي رد النص أو المفتاح أو تفاصيل الاستثناءات الداخلية.

- [x] تدقيق شامل: اختبار واجهة Create وReveal وتبديل العربية/الإنجليزية وسطح المكتب والهاتف والمسار المباشر.
  - ناتج القبول: نجح التدفق الكامل من إنشاء الرابط إلى Reveal ثم used، وفُحصت العربية والإنجليزية والرابط المفقود وشاشة 390×844 بلا تجاوز أفقي أو أخطاء console.

- [x] تدقيق شامل: مراجعة التشغيل الموحد وDockerfile وفشل الإعدادات الناقصة وفحص وجود أسرار في المستودع.
  - ناتج القبول: نجح تدفق الخدمة الواحدة وSPA و404 API وخطأ التحقق العام؛ فحص المستودع لم يجد أسرارًا متعقبة، وDockerfile يثبت البناء ويشغّل Uvicorn. لا يمكن بناء Docker محليًا في هذه البيئة.

- [x] تدقيق شامل: إصلاح كل عيب مثبت وإعادة مجموعة الاختبارات والتوثيق في `docs/08-audit.md`.
  - ناتج القبول: كل إصلاح مرتبط باختبار، ونجحت المجموعة النهائية (60 اختبار Python و2 Vitest)، وتوضح الوثيقة القيود التي لا يمكن تأكيدها محليًا.

- [x] عيب تدقيق مثبت: تثبيت استهلاك السر حتى عند فشل فك Base64 أو AES-GCM للبيانات التالفة.
  - ناتج القبول: يثبت اختبار API أن محاولة Reveal لبيانات تالفة تعيد 410 عامًّا ثم تظهر `used` بدل `active` في طلب الحالة اللاحق.

- [x] عيب تدقيق مثبت: تحويل `mysql://` إلى `mysql+pymysql://` داخل طبقة قاعدة البيانات من دون تغيير بيانات الاتصال.
  - ناتج القبول: يثبت الاختبار أن محرك SQLAlchemy يقبل `mysql://` عبر PyMySQL، بينما تبقى عناوين SQLite والعنوان المتوافق كما هي.

- [x] عيب تدقيق مثبت: جعل مسارات `/api/*` غير المعروفة تعيد 404 JSON بدل fallback صفحة React.
  - ناتج القبول: يثبت اختبار أن `GET /api/not-a-route` يعيد 404 JSON، بينما سيُعاد اختبار `/s/{id}` يدويًا ضمن تدقيق التشغيل الموحد.

- [x] عيب أمني مثبت: منع FastAPI/Pydantic من عكس `plaintext` في جسم خطأ التحقق 422.
  - ناتج القبول: يثبت الاختبار أن الرسالة الطويلة المرفوضة تعود كخطأ عام لا يحتوي النص أو حقل `input` أو تفاصيل تدل عليه.

- [x] عيب واجهة مثبت: مسح رسائل الخطأ والنسخ عند تبديل اللغة كي لا تظهر العربية داخل واجهة إنجليزية أو العكس.
  - ناتج القبول: بعد ظهور رسالة عربية للحقل الفارغ ثم تبديل الإنجليزية، اختفت الرسالة المؤقتة وظهرت الواجهة باتجاه LTR.

- [x] عيب محتوى مثبت: تحديث وصف الإنشاء والرابط في اللغتين ليصف صفحة Reveal الموجودة فعليًا بدل مرحلة مستقبلية.
  - ناتج القبول: ظهرت الصياغة المحدثة في الواجهة العربية والإنجليزية ولم تعد تقول إن صفحة Reveal ستُبنى لاحقًا.

- [x] عيب إعداد مثبت: استبدال قيمة `ONESECRET_ENCRYPTION_KEY` غير الصالحة بقيمة Base64 صالحة تمثل 32 بايت، ثم التحقق منها من بيئة المشروع دون طباعتها.
  - ناتج القبول: نجح اختبار تشغيل فعلي يقرأ المفتاح من بيئة المشروع ويبدأ FastAPI من دون كشف المفتاح في الكود أو Git أو الاستجابة.

- [x] تدقيق إعداد: إضافة اختبار Vitest يبدأ خدمة Python مع المفتاح المورّد ويستدعي health endpoint لا يعرض أي سر.
  - ناتج القبول: نجح Vitest؛ بدأ FastAPI بالمفتاح المورّد وأعاد health endpoint `{ "status": "ok" }` فقط.

- [x] إعداد المفتاح بطلب المستخدم: توليد قيمة AES-256 عشوائية جديدة وتسجيلها في إعدادات المشروع الآمنة دون عرضها أو حفظها محليًا.
  - ناتج القبول: استُخدم مولد تشفيري ثم حفظ المفتاح مباشرة في الإعداد السري؛ أثبت اختبار health صلاحيته من دون كشفه في ملفات المشروع.

- [x] ملاحظة نشر مثبتة: تثبيت إصدارات اعتماديات Python المباشرة في `requirements.txt` لضمان بناء قابل لإعادة الإنتاج.
  - ناتج القبول: حدد الملف إصدارات FastAPI وUvicorn وSQLAlchemy وPyMySQL وcryptography وpytest، ونجحت بيئة نظيفة في إعادة تثبيتها وتشغيل 60 اختبارًا.

- [x] عيب قابل لإعادة الإنتاج: تضمين اعتماد TestClient المطلوب في متطلبات Python حتى تنجح الاختبارات داخل بيئة نظيفة.
  - ناتج القبول: أضيف `httpx2` المحدد بالإصدار، وأنشئت بيئة جديدة من `requirements.txt` وحده ونجحت اختبارات FastAPI من دون حزم sandbox المسبقة.

- [x] تحسين تدقيق: جعل اختبار Vitest للتشغيل يختار منفذًا متاحًا وينهي عملية Python بأمان حتى عند فشل البدء.
  - ناتج القبول: اختار Vitest منفذًا حرًا ونجح اختبار health من دون منفذ 8017 ثابت أو مهلة hook.

- [x] تدقيق قاعدة الإنتاج: التحقق من وجود جدول `secrets` ومطابقة أعمدته للمخطط قبل النشر.
  - ناتج القبول: أنشئ جدول `secrets` بالحقول المشفرة والأوقات فقط، وفُحصت أعمدته وفهرسه، ثم نجحت دورة إنشاء وكشف عبر قاعدة الإنتاج وحُذف سجل التدقيق بعدها.

- [x] عيب إنتاج مثبت: تطبيع إعداد SSL في عنوان MySQL من منصة المشروع إلى صيغة تقبلها PyMySQL.
  - ناتج القبول: يبدأ FastAPI بإعدادات قاعدة البيانات الفعلية ويعيد health endpoint 200، من دون طباعة عنوان الاتصال أو بيانات اعتماده.

- [x] استكشاف المستخدم: تشغيل نسخة OneSecret الموحّدة على رابط مؤقت بقاعدة ومفتاح اختبار منفصلين.
  - ناتج القبول: أتيح رابط يعرض واجهة OneSecret الحقيقية ويعمل بقاعدة ومفتاح اختبار منفصلين عن الإنتاج.

- [x] إعادة تصميم UI/UX: اعتماد واجهة فاتحة ونظيفة موجهة للهاتف مع خط عربي واضح وزر داكن أنيق فقط.
  - ناتج القبول: تصبح الشاشة مفهومة من النظرة الأولى، وتبقى عناصر الإنشاء واللغة قابلة للوصول على الهاتف وسطح المكتب.

- [x] إعادة تصميم المحتوى: إزالة الشروح التقنية من واجهات المستخدم وإبقاء تعليمات قصيرة عملية فقط.
  - ناتج القبول: لا تظهر للمستخدم ادعاءات منع تصوير الشاشة أو تفاصيل تشفير أو تحقق لا يحتاج إليها.

- [x] إعادة تصميم Reveal: كشف السر تلقائيًا عند فتح رابط صالح وعرضه بحجم كبير وواضح مع حالات عامة للرابط غير المتاح.
  - ناتج القبول: لا يظهر زر Reveal، ويستهلك الرابط الصحيح مرة واحدة تلقائيًا من دون كشف النص قبل استجابة الخادم.

- [x] إعادة تصميم الهوية: إضافة تذييل قصير «تطوير الحارثة الداهية» في الواجهة.
  - ناتج القبول: يظهر التذييل بوضوح وبهدوء في الصفحات الرئيسية دون تزاحم مع المحتوى.

- [x] تدقيق قبل النسخة النهائية: فحص الأداء والاستجابة وسهولة القراءة والتدفق التلقائي للرسالة على الهاتف وسطح المكتب.
  - ناتج القبول: تنجح الاختبارات والبناء وتوثق نتائج الأداء والقيود وخطة الاختبار النهائية.

- [x] عيب تصميم مكتشف: استعادة استيراد Tailwind في CSS العام بعد إعادة التصميم.
  - ناتج القبول: عاد تخطيط Tailwind بعد البناء، وظهرت واجهة الإرسال بالعرض الصحيح مع المسافات والاستجابة المقصودة.

- [x] عيب تدفق مكتشف: منع إرسال Reveal التلقائي أكثر من مرة من الصفحة نفسها.
  - ناتج القبول: عند فتح رابط جديد، يظهر النص في أول تحميل بدل أن يستهلك الطلب الأول الرابط ثم تعرض الصفحة نتيجة used من طلب تالٍ.

- [x] إعادة تصميم نهائي: اعتماد هوية Light / Elegant / Minimal مع لمسة معدنية هادئة، من دون تعديل API أو التشفير أو منطق انتهاء الرابط.
  - ناتج القبول: يصبح التصميم أقرب لمنتج تقني احترافي، مع الحفاظ على الوظائف الحالية كما هي.

- [x] إعادة تصميم نهائي: تبسيط واجهة المرسل وحذف كل المعلومات التقنية غير الضرورية للمستخدم.
  - ناتج القبول: يرى المرسل الرسالة والمدة وزر إنشاء الرابط فقط، مع تذييل «Alhareith Aldahia» بشكل هادئ.

- [x] إعادة تصميم نهائي: جعل صفحة المستلم تتحقق تلقائيًا وتعرض الرسالة مرة واحدة بحجم مريح ومن دون زر أو تفاصيل تقنية.
  - ناتج القبول: تحافظ الصفحة على طلب Reveal واحد وعلى الحالات العامة للرابط غير المتاح.

- [x] أداء الواجهة: مراجعة CSS وJavaScript والطلبات الأولية لتقليل ما لا يلزم وتأخير ما لا يحتاجه المسار الأول.
  - ناتج القبول: يبقى بناء الواجهة خفيفًا وتظهر واجهة الإنشاء ورسالة المستلم بسرعة من دون مكتبات واجهات جديدة.

- [x] تحقق إعادة التصميم: اختبار الهاتف وسطح المكتب والعربية والإنجليزية والتدفق الكامل بعد تغيير الهوية.
  - ناتج القبول: لا تنكسر وظيفة إنشاء الرابط أو النسخ أو الكشف التلقائي أو صلاحية الرابط.

- [x] عيب تجربة مستخدم مكتشف: مسح تنبيه الرسالة الفارغة فور أن يبدأ المستخدم كتابة رسالة صالحة.
  - ناتج القبول: لا يبقى تنبيه «اكتب رسالة أولًا» ظاهرًا بعد إدخال نص غير فارغ، في العربية والإنجليزية.

- [x] تحديث تجربة الإرسال: إضافة مدة صلاحية أقصر من 5 دقائق مع إبقاء التحقق الأمني والخادم كما هما.
  - ناتج القبول: يستطيع المرسل اختيار مدة أقصر من 5 دقائق، وتظل القيمة مقبولة من FastAPI ضمن حدود الصلاحية الحالية.

- [x] تحديث الهوية: اعتماد خط عربي أثقل وواضح وألوان صافية مريحة من دون مؤثرات أو خلفيات ثقيلة.
  - ناتج القبول: تتحسن قراءة العناوين والرسائل بالعربية، وتصبح الخلفية والعناصر أكثر صفاءً مع الحفاظ على خفة الواجهة.

- [x] تحديث مشاركة الرابط: اختصار الرابط المعروض وإضافة زر مشاركة أصلي بجانب زر النسخ.
  - ناتج القبول: لا يظهر الرابط كاملًا داخل بطاقة النجاح، بينما ينسخ ويشارك الرابط الكامل عبر التطبيقات التي يدعمها الجهاز.

- [x] تحديث صفحة المستلم: إضافة دعوة صغيرة بعد عرض الرسالة للانتقال إلى إنشاء رسالة جديدة.
  - ناتج القبول: تظهر دعوة Minimal ثنائية اللغة فقط بعد نجاح العرض، وتنقل إلى `/` من دون حسابات أو Popup أو تغيير في كشف الرسالة.

- [x] تحسين دعوة المستلم: دمج سطري الدعوة في CTA واحد قابل للنقر.
  - ناتج القبول: يظهر نص تمهيدي صغير وسطر إجراء واضح مع سهم داخل رابط واحد، من دون تكرار أو ازدحام بصري.

- [x] عيب مشاركة مكتشف: عند غياب مشاركة الجهاز لا يتحول زر المشاركة إلى نسخ فقط من دون اختيار مشاركة واضح.
  - ناتج القبول: يستدعي زر المشاركة ورقة الجهاز عند دعمها، أو يعرض خيارات إرسال الرابط والنسخ في المتصفحات غير الداعمة.

- [x] تحسين مشاركة مكتشف: إغلاق لوحة الخيارات البديلة قبل استدعاء مشاركة الجهاز.
  - ناتج القبول: لا تبقى خيارات واتساب وتلغرام والبريد ظاهرة إذا عاد المستخدم إلى مسار مشاركة الجهاز.

- [x] تعديل سلوك الصلاحية: جعل الرسائل قابلة للعرض عدة مرات حتى `expires_at` افتراضيًا بدل استهلاكها عند أول فتح.
  - ناتج القبول: يعيد الرابط النص الصحيح في أي عدد من مرات الفتح قبل الانتهاء، ثم يعيد حالة عامة بعد الانتهاء.

- [x] خيار اختياري: إضافة «إتلاف بعد أول فتح» عند الإنشاء مع بقاء الإتلاف معطلًا افتراضيًا.
  - ناتج القبول: لا يصبح الرابط لمرة واحدة إلا عند اختيار المرسل الصريح للخيار، وتظل الرسائل الافتراضية قابلة للعرض حتى الانتهاء.

- [x] Secret Code اختياري: حماية فتح الرسالة بكود لا يُخزن كنص صريح ولا يظهر في الرابط أو السجلات.
  - ناتج القبول: يطلب المستلم الكود الصحيح عند الحاجة، ويرفض الكود الخاطئ برسالة عامة، وتعمل الرسالة بالكود عدة مرات حتى الانتهاء ما لم يُفعل الإتلاف.

- [x] اختبار دورة الرسالة: التحقق من العرض المتكرر، والانتهاء، والإتلاف الاختياري، وSecret Code، ومنع الوصول بعد انتهاء الصلاحية.
  - ناتج القبول: تغطي اختبارات Python والواجهة الحالات التسع المطلوبة من المستخدم من دون تغيير غير مطلوب لنموذج التشفير الحالي.

- [x] عيب إتلاف مكتشف: لا يجوز أن تعيد الواجهة عرض نتيجة Reveal مخزنة لرابط اختير له الإتلاف بعد أول فتح.
  - ناتج القبول: بعد نجاح أول فتح لرابط الإتلاف، تعيد أي زيارة لاحقة طلبًا جديدًا وتظهر حالة عامة غير متاحة.

- [x] تعديل مشاركة الهاتف: استخدام Web Share API / نافذة المشاركة الأصلية فقط، وإزالة روابط واتساب وتلغرام والبريد المخصصة من الواجهة.
  - ناتج القبول: يفتح زر المشاركة نافذة النظام على الأجهزة الداعمة؛ لا تظهر تطبيقات أو روابط مشاركة داخل الموقع، ويبقى زر النسخ مستقلًا للمتصفحات غير الداعمة.

- [x] تسليم أرشيف المشروع: إنشاء ملف ZIP من كود OneSecret وتوثيقه واختباراته، مع استبعاد الأسرار و`node_modules` و`.venv` وملفات قواعد البيانات المؤقتة.
  - ناتج القبول: ملف قابل للتنزيل يحتوي الشيفرة والتوثيق وملفات الإعداد اللازمة، من دون مفاتيح أو ملفات اعتماديات ضخمة.

- [x] اعتماد الإصدار الأولي V1: توثيق نطاق النسخة الحالية وحدودها ومعايير عدم تغييرها بلا قرار جديد.
  - ناتج القبول: وثيقة مرجعية تحدد ما يقدمه V1 وما يؤجل عمدًا، وتفصل بين التحسينات المستقبلية وتغيير نموذج الأمان.

- [x] خارطة تطوير OneSecret: تحليل المسارات التالية بحسب قيمة المستخدم والأمان وتعقيد مشروع المقرر وترتيبها على مراحل.
  - ناتج القبول: خطة عملية لا تضيف ميزات عشوائية، تحدد الأولوية التالية وأسبابها وما لا يدخل حاليًا.

- [x] قرار خارطة الطريق: اعتماد الصور كمرحلة مستقلة بعد V1.1 للتحصين وV1.2 لإلغاء الرابط، لا كإضافة إلى V1 الحالي.
  - ناتج القبول: توثق الخارطة مرحلة صور صغيرة وآمنة بنطاق واضح، ولا يبدأ تنفيذها قبل إنجاز V1.1 وV1.2.

- [x] V1.1: تحليل وتنفيذ حدود طلبات لإنشاء الرسائل وفتحها ومحاولات Secret Code، مع سجل أمني لا يخزن رسالة أو Code أو رابطًا كاملًا.
  - ناتج القبول: تمنع الحدود التخمين والإساءة مؤقتًا من دون إعاقة الاستخدام العادي أو تسريب محتوى حساس.

- [x] V1.1: إضافة رؤوس حماية تمنع تخزين استجابات الرسائل الحساسة وتقلل مخاطر المتصفح.
  - ناتج القبول: استجابات Reveal تستخدم `Cache-Control: no-store`، وتبقى مشاركة الهاتف الأصلية صالحة، وتُختبر الرؤوس في FastAPI.

- [x] V1.1: اختبار وتوثيق الحالة الطبيعية وتجاوز الحدود والمحاولات الخاطئة للكود.
  - ناتج القبول: تغطي pytest الحالات المقبولة والمرفوضة، وتثبت أن السجل لا يحوي النص أو Secret Code أو معرّف الرابط الكامل.

- [x] تحسين أمني V1.1: اشتقاق مفتاح محدد الطلبات من المصدر ونطاق العملية معًا بدل الاحتفاظ بمعرّف الرابط داخل مفتاح ذاكرة ظاهر.
  - ناتج القبول: لا يحتوي مخزن الحدود في الذاكرة على `secret_id` خام، وتبقى حدود Secret Code مستقلة لكل رابط.

- [x] تجربة V1.1: إظهار رسالة انتظار عامة عند `429` في الإنشاء والكشف التلقائي وإدخال Secret Code بدل وصفها بخطأ عام أو كود خاطئ.
  - ناتج القبول: تبقى أخطاء الكود الخاطئ عامة، بينما يفهم المرسل أو المستلم أن عليه الانتظار عند تجاوز الحد، من دون عرض تفاصيل الحد أو الرابط.

- [x] تحسين تشغيلي V1.1: تنظيف مفاتيح الحدود المنتهية من الذاكرة حتى لا تتراكم طلبات مصادر كثيرة بمرور الوقت.
  - ناتج القبول: تزال سجلات النوافذ المنتهية دوريًا، ولا يحتفظ المحدد بمصادر انتهت كل نوافذها.

- [x] سجل V1.1: تعطيل سجل وصول Uvicorn الافتراضي في صورة الإنتاج لمنع تسجيل مسارات روابط الأسرار في stdout.
  - ناتج القبول: لا يسجل الخادم المسار أو معرّف الرابط تلقائيًا، بينما تبقى أحداث الحظر المختصرة الخالية من المحتوى متاحة للمشغل.

- [x] GitHub: تجهيز مستودع OneSecret عام واحترافي، README عربي وصور موثوقة، وإصدارين V1.0 وV1.1.
  - ناتج القبول: يكون المستودع والإصداران والتوثيق متسقين مع تاريخ المشروع، ولا تُرفع أسرار أو ملفات بيئة أو بيانات اختبار.

- [x] GitHub: إضافة بوابة تحقق عامة للواجهة والخادم، وتثبيت حقوق الاستخدام كـ`UNLICENSED` بدل الادعاء برخصة MIT مفتوحة.
  - ناتج القبول: يختبر GitHub الواجهة والخادم من اعتماديات مقفلة، ولا يصرح المشروع ضمنيًا بإعادة استخدام الشيفرة رغم كونه مرئيًا للعامة.

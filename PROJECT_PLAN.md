<div dir="rtl" align="right">

# OneSecret — خطة تثبيت V2

## الهدف الحالي

الهدف ليس إضافة خوارزمية جديدة، بل تحويل الحالة الحالية إلى نسخة نهائية واضحة وقابلة للدفاع عنها أكاديميًا وتقنيًا، مع إزالة التعارض بين الكود الحديث والتوثيق القديم.

## المعمارية المعتمدة حاليًا

| النوع | التشفير | مكان التنفيذ |
|---|---|---|
| النصوص | AES-256-GCM | المتصفح عبر Web Crypto |
| الملفات | AES-256-GCM + RSA-OAEP/SHA-256 | المتصفح عبر Web Crypto |
| الصور | Single DES-CBC + PKCS#7 | Backend لأغراض تعليمية |
| البيانات | SQLAlchemy + SQLite محليًا / MySQL-TiDB عند النشر | Backend |
| الواجهة | React + TypeScript + Tailwind CSS | Browser |
| API | FastAPI | Backend |

## قاعدة العمل

> لا نضيف ميزات جديدة قبل إنهاء مراحل التثبيت التالية بالترتيب، ولا ننتقل إلى مجموعة مراحل جديدة قبل اكتمال المجموعة الحالية واختبارها ورفعها للمراجعة.

## المرحلة A — توحيد الحقيقة التقنية والتوثيق

**الحالة:** قيد التنفيذ على `cleanup/v2-finalization`.

المطلوب:

- إعادة كتابة README وفق المعمارية الحالية.
- تحديث SECURITY حسب Trust Boundary لكل نوع بيانات.
- تحديث CONTRIBUTING كي لا يفرض نموذج Server-side القديم.
- توضيح أن الوثائق المرحلية القديمة تاريخية وليست مرجعًا نهائيًا.
- توثيق تشغيل Windows CMD بوضوح.
- تثبيت خطة V2 قبل أي تغيير سلوكي.

**معيار القبول:** لا يوجد في التوثيق الحالي ادعاء بأن كل التشفير يحدث في Backend أو أن الصور Client-side أو أن RSA يشفر الملف كاملًا.

## المرحلة B — استعادة الحماية للمسارات الجديدة

المطلوب:

- نقل حدود محاولات Secret Code الخاطئة إلى Client Text API.
- نقل حدود محاولات Cancel Code الخاطئة إلى Client Text API.
- الحفاظ على رسائل خطأ عامة وعدم كشف حالة سرية إضافية.
- إضافة اختبارات لهذه الحدود.
- فحص Magic Bytes لصور PNG/JPEG قبل DES encryption، وليس الاعتماد على MIME فقط.
- إضافة اختبارات للملف الذي يدعي MIME صورة ولا يحمل signature صحيحة.

**معيار القبول:** Client Crypto لا يفقد حماية التخمين التي كانت موجودة في المسار القديم، وDES لا يقبل ملفًا مزيفًا اعتمادًا على `Content-Type` فقط.

## المرحلة C — حسم Legacy APIs

المطلوب:

- جرد كل Server-side AES/RSA endpoint القديم.
- تحديد ما إذا كان مطلوبًا فعلًا لروابط قديمة.
- فصل demo endpoints عن production routes.
- تعطيل أو حذف ما لا يستخدم بعد إثبات ذلك بالبحث والاختبارات.
- إزالة الاعتماد على `ONESECRET_ENCRYPTION_KEY` من التشغيل الجديد إذا لم تعد أي Compatibility path تحتاجه.

**معيار القبول:** يوجد مسار أساسي واحد واضح لكل نوع بيانات، ولا يبقى Legacy endpoint مفعّلًا بلا سبب موثق.

## المرحلة D — اختبارات Browser Crypto وتنظيف الاعتماديات

المطلوب:

- إضافة اختبار round-trip للنص AES-GCM داخل بيئة Frontend مناسبة.
- اختبار فشل key خاطئ وciphertext معدل وnonce غير صالح.
- اختبار Hybrid file round-trip وفشل RSA private key الخاطئ.
- إضافة test script فعلي للواجهة وتشغيله في GitHub Actions.
- إزالة duplicate dependencies وفصل runtime عن dev إذا كان ذلك مناسبًا.
- تثبيت Python المدعوم رسميًا بناءً على CI، بدل عبارة عامة غير مختبرة.

**معيار القبول:** أهم التشفير الحالي في المتصفح مغطى باختبارات آلية، والبيئة قابلة لإعادة البناء بدون اعتماديات زائدة أو متعارضة.

## المرحلة E — قاعدة البيانات ودورة حياة البيانات

المطلوب:

- مراجعة الجداول الجديدة: `client_text_shares`, `client_file_shares`, `des_image_shares`.
- اعتماد migrations واضحة بدل الاعتماد المستقبلي على `create_all()` وحده إذا تقرر نشر المشروع فعليًا.
- إضافة طريقة تنظيف للسجلات المنتهية التي لم يطلبها أحد.
- مراجعة one-time semantics للملفات والصور تحت التزامن.

**معيار القبول:** schema ودورة الحذف موثقتان ويمكن ترقيتهما بدون غموض.

## المرحلة F — توحيد تجربة الواجهة

المطلوب:

- مدخل واضح للاختيار بين Text / File / Image.
- توحيد اللغة والتصميم والتنقل بين الصفحات.
- استخدام تسمية Hybrid Encryption للملفات بدل الإيحاء بأن RSA يشفر الملف كاملًا.
- عدم تغيير منطق التشفير أثناء تحسين العرض إلا عند الحاجة وباختبار مستقل.

**معيار القبول:** المستخدم والمناقش يستطيعان فهم الأنواع الثلاثة ومساراتها من الواجهة نفسها.

## المرحلة G — إصدار V2 النهائي

المطلوب:

- تحديث رقم الإصدار بعد اكتمال كل ما سبق فقط.
- تحديث CHANGELOG وتوثيق مساهمات الفريق دون ادعاءات غير موثقة.
- تنظيف الفروع المدمجة بعد التحقق أن commits المهمة reachable من `main`.
- الإبقاء على نسخة احتياطية حتى نجاح الإصدار.
- تشغيل CI كامل على المرشح النهائي.
- إنشاء Tag/Release `v2.0.0` فقط بعد نجاح بوابة الجودة.

**معيار القبول:** README وSECURITY والكود والاختبارات والإصدار تصف النظام نفسه بلا تعارض.

## ما لا يدخل الآن

- حسابات مستخدمين.
- دردشة.
- ذكاء اصطناعي.
- خوارزمية تشفير رابعة.
- إعادة تصميم كاملة غير مرتبطة بالتسليم.
- ادعاءات تسويقية مثل Zero Knowledge للمشروع كله.

</div>

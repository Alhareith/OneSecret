# المساهمة في OneSecret

هذا المستودع في مرحلة تثبيت المعمارية الحالية قبل إصدار V2. الهدف الآن هو **التنظيف، الحماية، الاختبار والتوثيق** لا إضافة ميزات جديدة غير مرتبطة.

## قبل أي تعديل

راجع بالترتيب:

1. `README.md` لفهم المعمارية الحالية.
2. `SECURITY.md` لمعرفة حدود الثقة الحقيقية.
3. `PROJECT_PLAN.md` لمعرفة مرحلة التثبيت الحالية.
4. `docs/19-v2-finalization.md` لمعرفة معايير القبول قبل الإصدار.

## قواعد التشفير الحالية

- النصوص تُشفّر وتُفك داخل المتصفح باستخدام AES-256-GCM.
- الملفات تُشفّر داخل المتصفح باستخدام Hybrid Encryption: AES-256-GCM للمحتوى وRSA-OAEP/SHA-256 لمفتاح AES.
- الصور تستخدم Single DES-CBC + PKCS#7 في Backend كمسار تعليمي مستقل.
- لا ترسل plaintext للنصوص أو الملفات إلى Backend في المسارات الجديدة.
- لا تنقل DES image encryption إلى Web Crypto ضمن تنظيف عادي؛ أي تغيير في مكانه يحتاج قرارًا معماريًا منفصلًا.
- لا تدّع أن RSA يشفر الملف كاملًا.
- لا تدّع أن DES اختيار أمني حديث.

## الأسرار والبيانات الحساسة

ممنوع رفع أو تسجيل:

- مفاتيح AES أو RSA private keys أو DES keys الحقيقية.
- `ONESECRET_ENCRYPTION_KEY`.
- Database credentials.
- Secret Code أو Cancel Code أو claim token حقيقي.
- رابط مشاركة صالح كامل.
- ملفات مستخدمين أو صور أو نصوص حقيقية داخل الاختبارات.

استخدم بيانات اختبار مولدة فقط.

## الاختبارات المطلوبة

أي تغيير وظيفي يجب أن يضيف أو يحدّث اختبارًا يثبت السلوك المقصود ولا يكسر المسارات الأخرى.

Backend:

```bash
cd backend
pytest -q
```

Frontend:

```bash
cd frontend
pnpm check
pnpm build
```

عند إضافة اختبارات Web Crypto في مرحلة لاحقة يجب أن تصبح جزءًا من بوابة الجودة كذلك.

## أسلوب العمل

- اعمل في branch مستقل عن `main`.
- اجعل كل مرحلة مرتبطة بهدف واحد واضح.
- لا تجمع تنظيفًا كبيرًا مع تغيير تشفيري جوهري في commit واحد.
- لا تحذف Legacy code قبل إثبات أنه غير مستخدم أو قبل توفير مسار توافق واضح.
- قبل الدمج قارن branch مع `main` وافحص الملفات المتغيرة ومخرجات CI.
- لا تستخدم force push على `main`.

## التوثيق

إذا تغير Trust Boundary أو مكان تنفيذ التشفير أو شكل envelope أو طريقة إدارة المفاتيح، يجب تحديث `README.md` و`SECURITY.md` والتوثيق التقني في نفس Pull Request.

الوثائق `docs/01-...` إلى `docs/18-...` تسجل مراحل تاريخية وقد لا تصف المعمارية الحالية. لا تعتمد عليها وحدها لشرح النظام الحالي؛ استخدم `docs/README.md` و`docs/19-v2-finalization.md` لتحديد المرجع الفعلي.

## تعريف الإنجاز

لا تعتبر المهمة مكتملة لمجرد أن الكود يعمل محليًا. يجب أن تكون:

- صحيحة وظيفيًا.
- متوافقة مع نموذج الأمان الحالي.
- مغطاة باختبار مناسب.
- موثقة إذا غيرت سلوكًا أو عقدًا.
- ناجحة في GitHub Actions قبل الدمج.

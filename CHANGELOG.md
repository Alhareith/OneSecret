# سجل إصدارات OneSecret

يسجل هذا الملف التغييرات المعتمدة في الإصدارات المنشورة. تكتب التغييرات من منظور ما يهم مستخدم المشروع ومراجعه، ولا يتضمن بيانات اختبار أو مفاتيح أو روابط أسرار صالحة.

## [1.1.1] — 2026-08-27

### غُيّر

- تحسين بوابة GitHub Actions لضمان إعداد pnpm قبل تفعيل ذاكرة التخزين المؤقت لـNode.js، ثم نجاح التحقق للواجهة والخادم.
- تنقيح README العام ليعرض المنصة كمنتج تقني مستقل، من دون صياغة موجهة لمقرر أو مناقشة أكاديمية.
- إضافة سجل للنشر العام والإصدارات، وتحديث رقم الإصدار في حزمة الواجهة وواجهة FastAPI إلى `1.1.1`.

### لم يتغير

- لا يوجد تغيير وظيفي في التشفير أو الواجهة أو مسارات API أو سلوك الصلاحية وSecret Code. يبقى هذا إصدار صيانة وتوثيق فوق تحصين V1.1.0.

## [1.1.0] — 2026-08-27

### أضيف

- محدد طلبات داخل FastAPI لإنشاء الرسائل وReveal ومحاولات Secret Code الخاطئة.
- مفاتيح HMAC داخلية لمحدد الطلبات، مع تنظيف دوري للمفاتيح المنتهية من الذاكرة.
- رؤوس متصفح: `X-Content-Type-Options` و`X-Frame-Options` و`Referrer-Policy` وCSP و`Permissions-Policy` مع إبقاء مشاركة الهاتف الأصلية مفعلة.
- `Cache-Control: no-store` لمسارات الرابط وحالته ونتائج Reveal الحساسة.
- رسائل انتظار عربية وإنجليزية واضحة لاستجابة `429` في الإنشاء والكشف وSecret Code.
- اختبارات للوحدة وAPI تغطي انتهاء نوافذ الحد وعزل النطاقات والرؤوس لحالات `200` و`401` و`410` و`429`.
- بوابة جودة GitHub Actions، وسياسات مساهمة وأمان للمستودع العام.

### غُيّر

- تشغيل الإنتاج يعطّل سجل وصول Uvicorn الافتراضي؛ يمنع ذلك طباعة مسارات روابط الأسرار في stdout.
- توثيق المشروع يوضح صراحة أن محدد الطلبات داخل الذاكرة لكل نسخة خادم، وأن إعداد proxy الموثوق يحتاج تحققًا قبل تشغيل عدة نسخ.

### التحقق

| المسار | النتيجة |
|---|---|
| Python / FastAPI | `71 passed` في pytest |
| React / TypeScript | فحص الأنواع ناجح |
| Vite | بناء الإنتاج ناجح |
| اختبارات بيئة المشروع | `3 passed` في Vitest |
| HTTP والمتصفح | التحقق من الحظر والرؤوس والسجل الآمن ناجح |

## [1.0.0] — 2026-08-27

### أضيف

- إنشاء رسائل نصية مؤقتة ثنائية اللغة بالعربية RTL والإنجليزية LTR.
- تشفير AES-256-GCM داخل خادم Python مع `nonce` جديد لكل عملية؛ لا تحفظ قاعدة البيانات النص الأصلي.
- رابط عشوائي صالح حتى `expires_at`، مع مدد من دقيقة إلى 24 ساعة.
- عرض متكرر افتراضي حتى انتهاء الرابط، وخيار مستقل للإتلاف بعد أول فتح.
- Secret Code اختياري مشتق بـscrypt مع salt عشوائي؛ لا يظهر في الرابط ولا يحفظ كنص أصلي.
- مشاركة هاتف أصلية باستخدام Web Share API وزر نسخ مستقل، مع تجربة مستلم خفيفة وCTA للإنشاء.
- توثيق مرحلي يشرح المعمارية وقرارات الأمان والاختبارات وخارطة الطريق.

### التحقق

تأكد الإصدار الأول من مسارات إنشاء الرسالة والكشف المتكرر والانتهاء والإتلاف وSecret Code والتزامن، مع فحص TypeScript وبناء Vite ومعاينة الواجهة العربية والإنجليزية.

[1.1.1]: https://github.com/Alhareith/OneSecret/releases/tag/v1.1.1
[1.1.0]: https://github.com/Alhareith/OneSecret/releases/tag/v1.1.0
[1.0.0]: https://github.com/Alhareith/OneSecret/releases/tag/v1.0.0
